# ipnc

## Usage

```
  -i string
    	File with CIDR notation networks, new line separated each
  -s string
    	Single IP-address to search
  -sf string
    	File with IPs to search, new line separated each
```
